unit System
{
	uses Constants;
	
	interface
	{
		native[SYSTEM]:
		{
			function sleep(int time);
			
			function yield();
			
			function gc();
			
			function currentTimeMillis():int;
			
			function arrayCopy(object src, int srcPos, object dest, int destPos, int length);
		}
	}
}